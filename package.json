{
  "name": "sarah_pilates_root",
  "private": true,
  "workspaces": [
    "clean-sarahs-pilates-client",
    "server"
  ],
  "scripts": {
    "postinstall": "npm --prefix clean-sarahs-pilates-client install && npm --prefix server install",
    "build": "npm --prefix clean-sarahs-pilates-client run build && npm --prefix server run build",
    "start": "node server/dist/index.js"
  },
  "engines": {
    "node": ">=18.18"
  },
  "type": "module",
  "dependencies": {}
}
